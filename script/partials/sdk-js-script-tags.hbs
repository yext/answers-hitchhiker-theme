{{!--
  Returns the SDK js script tags needed by the page.
  @param locale 
  @param sdkVersion
--}}
<script
  src="{{sdkAssetUrl assetName="answers-modern.min.js" locale=locale branch=sdkVersion}}"
  type="module"
  onload="initAnswers()"
></script>
<script
  src="{{sdkAssetUrl assetName="answers.min.js" locale=locale branch=sdkVersion}}"
  nomodule
  onload="initAnswers()"
  defer
></script>