name: WCAG tests

on: push

jobs:
  call_WCAG_test:
    uses:  yext/answers-headless/.github/workflows/wcag_test_reuse.yml@dev/test-reusable-workflows
    with:
      build_script: 'npm run setup-test-site && npm run build-test-site'
