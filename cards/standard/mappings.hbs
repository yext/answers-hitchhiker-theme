{
  title: (item) => {
    return {{#if title.value}}`{{title.value}}`{{else}}{{#if title.field}}item.{{title.field}}{{else}}item.title{{/if}}{{/if}};
  },
  details: (item) => {
    return {{#if details.value}}{{details.value}}{{else}}{{#if details.field}}item.{{details.field}}{{else}}item.details{{/if}}{{/if}};
  },
  subtitle: (item) => {
    return {{#if subtitle.value}}{{subtitle.value}}{{else}}{{#if subtitle.field}}item.{{subtitle.field}}{{else}}item.subtitle{{/if}}{{/if}};
  }
}